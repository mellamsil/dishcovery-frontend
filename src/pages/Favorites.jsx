import React from "react";

const Favorites = () => {
  return (
    <div className="favorites container">
      <h1 className="favorites__title">Your Cookbook</h1>
      <p className="favorites__intro">
        Saved recipes will appear here (requires backend).
      </p>
    </div>
  );
};

export default Favorites;
